from linkedlist import LinkedList, Node, printList


def deleteEntireLinkedList(head: Node):
    head = None
    return head

list = LinkedList([3, 1, 2, 5])
list = deleteEntireLinkedList(list.head)
printList(list)